<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:zxing="clr-namespace:ZXing.Net.Maui.Controls;assembly=ZXing.Net.MAUI.Controls"
             x:Class="MauiApp1.NouvelleLivraison"
             Title="Nouvelle Livraison">
    <Grid>
        <!-- Overlay principal -->
        <StackLayout x:Name="MainContent" Padding="20" Spacing="15" VerticalOptions="Center">
            <Label Text="Résultat du Scan" FontSize="20" FontAttributes="Bold" HorizontalOptions="Center" />
            <Frame x:Name="ResultFrame" IsVisible="False" Padding="15" CornerRadius="10" BorderColor="LightGray" HasShadow="True">
                <Label x:Name="ResultLabel" FontSize="16" HorizontalOptions="Center" />
            </Frame>
            <Button x:Name="ValidateButton" Text="Valider le code" IsEnabled="False" Clicked="OnValidateClicked" HorizontalOptions="Fill" />
            <Button x:Name="RestartButton" Text="Recommencer le scan" Clicked="OnRestartClicked" HorizontalOptions="Fill" />
            <Button x:Name="BackButton" Text="Retour" Clicked="OnBackClicked" HorizontalOptions="Fill" BackgroundColor="Transparent" TextColor="Gray" BorderColor="Transparent"/>
        </StackLayout>

        <!-- Caméra dans un carré centré -->
        <zxing:CameraBarcodeReaderView
            x:Name="cameraView"
            IsVisible="True"
            BarcodesDetected="OnBarcodesDetected"
            WidthRequest="300"
            HeightRequest="300"
            HorizontalOptions="Center"
            VerticalOptions="Center"
            BackgroundColor="Black" />
    </Grid>
</ContentPage>